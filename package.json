{
  "name": "hudlTests",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "playwright test -c tests/ --headed --browser=chromium --workers=1 --timeout=60000",
    "test:all:headed" : "playwright test -c tests/ --headed --browser=all --workers=1 --timeout=60000",
    "test:all" : "playwright test -c tests/ --browser=all --workers=1 --timeout=60000"

  },
  "dependencies": {
    "@playwright/test": "1.19.1",
    "faker": "4.1.0",
    "chai-subset": "1.6.0",
    "prettier": "2.0.5",
    "randomstring": "1.1.5"
  }
}
